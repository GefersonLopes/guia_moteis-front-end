export interface InterfaceContextLogic {
  loading: () => void;
  noLoading: () => void;
  isLoad: boolean;
}

export interface ICardCortesias {
  img: string;
  title: string;
  description: string;
}
